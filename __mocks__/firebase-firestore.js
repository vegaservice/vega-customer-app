const serverTimestamp = jest.fn(() => new Date());
const mockUpdate = jest.fn(() => Promise.resolve());
const mockSet = jest.fn(() => Promise.resolve());
const mockGet = jest.fn(() => Promise.resolve({ exists: true, data: () => ({ walletBalance: 100 }), id: 'test' }));
const mockCollectionGet = jest.fn(() => Promise.resolve({ docs: [] }));
const mockAdd = jest.fn(() => Promise.resolve({ id: 'newId' }));
const mockOnSnapshot = jest.fn();
const mockDoc = jest.fn(() => ({ get: mockGet, update: mockUpdate, set: mockSet, onSnapshot: mockOnSnapshot, collection: jest.fn(() => ({ doc: mockDoc, add: mockAdd, get: mockGet })) }));
const mockCollection = jest.fn(() => ({ doc: mockDoc, add: mockAdd, where: jest.fn().mockReturnThis(), orderBy: jest.fn().mockReturnThis(), limit: jest.fn().mockReturnThis(), get: mockCollectionGet }));
const firestore = jest.fn(() => ({ collection: mockCollection }));
firestore.FieldValue = { serverTimestamp };
module.exports = firestore;
module.exports.default = firestore;
